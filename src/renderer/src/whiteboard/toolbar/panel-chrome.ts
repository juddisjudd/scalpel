/** Shared pill / panel chrome used by toolbar rows and the snapshot popovers.
 *  Whiteboard overlays use the design handoff's 22px panel rounding (vs the
 *  10px global `--radius` used elsewhere in the app for buttons / cards). */
export const PANEL_CHROME = 'bg-bg-card-translucent border border-border rounded-[22px] shadow-lg'
